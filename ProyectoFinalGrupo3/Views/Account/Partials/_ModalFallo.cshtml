<div id="loginFailModal" class="hidden fixed inset-0 z-50 flex items-center justify-center p-4 bg-background-dark/80 backdrop-blur-sm">
    <div class="w-full max-w-sm bg-background-dark border border-red-900/50 rounded-xl p-8 shadow-2xl relative overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-1 bg-red-600/50"></div>
        <div class="flex flex-col items-center text-center">
            <div class="w-16 h-16 bg-red-950/30 rounded-full flex items-center justify-center mb-6 border border-red-900/30">
                <span class="material-symbols-outlined text-red-500 text-4xl">error</span>
            </div>
            <h3 class="text-xl font-bold text-white mb-3">Error al Ingresar</h3>
            <p class="text-slate-400 text-sm leading-relaxed mb-8">
                Las credenciales no coinciden. Por favor, verifica tu correo y contraseña e intenta nuevamente.
            </p>
            <button onclick="closeModal('loginFailModal')" class="w-full bg-primary hover:bg-primary/90 text-white font-bold py-3 px-6 rounded-lg transition-all shadow-lg shadow-primary/10">
                Reintentar
            </button>
        </div>
    </div>
</div>
